# DE-Idle

A short incremental/idle game where the goal is to make cool graphs using the resources available to you!

## Building from source

This project is using Rust compiled to Wasm, packaged using [Trunk](https://crates.io/crates/trunk).

```shell
trunk build
```
